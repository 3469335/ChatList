# 🔧 Быстрое исправление 404 на GitHub Pages

## Проблема
`https://3469335.github.io/ChatList/` возвращает ошибку 404

## ✅ Решение (пошагово)

### Шаг 1: Закоммитьте изменения
```bash
git add docs/index.html
git commit -m "Fix GitHub Pages links"
git push origin main
```

### Шаг 2: Настройте GitHub Pages

1. Перейдите на GitHub: https://github.com/3469335/ChatList/settings/pages
2. В разделе **"Source"** выберите:
   - **"Deploy from a branch"** (если используете старый метод)
   - ИЛИ **"GitHub Actions"** (если используете workflow)
3. Если выбрали "Deploy from a branch":
   - **Branch**: `main`
   - **Folder**: `/docs`
4. Нажмите **"Save"**

### Шаг 3: Запустите GitHub Actions (если используете)

1. Перейдите: https://github.com/3469335/ChatList/actions
2. Найдите workflow **"Deploy to GitHub Pages"**
3. Нажмите **"Run workflow"** → **"Run workflow"**
4. Дождитесь завершения (зеленая галочка)

### Шаг 4: Проверка

1. Подождите 1-2 минуты
2. Откройте: https://3469335.github.io/ChatList/
3. Если все еще 404, очистите кэш браузера (Ctrl+F5)

## 🔍 Альтернативное решение

Если `/docs` не работает, можно использовать корневую директорию:

1. Скопируйте `docs/index.html` в корень:
   ```bash
   cp docs/index.html index.html
   git add index.html
   git commit -m "Add index.html to root for GitHub Pages"
   git push origin main
   ```

2. В настройках Pages выберите:
   - **Branch**: `main`
   - **Folder**: `/ (root)`

## 📝 Проверка статуса

1. Settings → Pages
2. Внизу страницы будет показан статус последнего деплоя
3. Если есть ошибки, они будут отображены там
