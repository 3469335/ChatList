# Исправление ошибки 404 на GitHub Pages

## 🔍 Диагностика проблемы

Если `https://3469335.github.io/ChatList/` возвращает 404, проверьте следующее:

## ✅ Решение 1: Проверка настроек GitHub Pages

1. Перейдите в ваш репозиторий на GitHub
2. Settings → Pages
3. Проверьте настройки:
   - **Source**: Должно быть выбрано "Deploy from a branch" или "GitHub Actions"
   - **Branch**: Если выбрано "Deploy from a branch", должна быть выбрана ветка `main` (или `master`)
   - **Folder**: Должна быть выбрана `/docs` (если используете директорию docs)

## ✅ Решение 2: Проверка наличия файлов в репозитории

Убедитесь, что файлы закоммичены и отправлены на GitHub:

```bash
# Проверьте, что docs/index.html существует в репозитории
git ls-files docs/

# Если файлов нет, добавьте их:
git add docs/
git commit -m "Add GitHub Pages files"
git push origin main
```

## ✅ Решение 3: Использование GitHub Actions (рекомендуется)

Если у вас настроен `.github/workflows/pages.yml`, убедитесь что:

1. Workflow запустился:
   - Перейдите в Actions → Deploy to GitHub Pages
   - Проверьте, что последний запуск завершился успешно (зеленая галочка)

2. Если workflow не запускается:
   - Перейдите в Actions
   - Нажмите "Run workflow" → "Run workflow"
   - Это запустит деплой вручную

## ✅ Решение 4: Альтернативный способ - корневая директория

Если `/docs` не работает, можно переместить файлы в корень:

1. Скопируйте `docs/index.html` в корень репозитория
2. В настройках Pages выберите Folder: `/ (root)`
3. Сохраните

## ✅ Решение 5: Создание ветки gh-pages

Альтернативный метод:

```bash
# Создайте ветку gh-pages
git checkout --orphan gh-pages
git rm -rf .
cp docs/index.html .
git add index.html
git commit -m "Initial GitHub Pages"
git push origin gh-pages

# Вернитесь на main
git checkout main
```

Затем в Settings → Pages выберите:
- Source: `gh-pages` branch
- Folder: `/ (root)`

## 🔧 Быстрое исправление (если ничего не помогает)

1. Убедитесь, что файл `docs/index.html` существует и закоммичен
2. Перейдите в Settings → Pages
3. Измените Source на "Deploy from a branch"
4. Выберите Branch: `main`, Folder: `/docs`
5. Сохраните
6. Подождите 1-2 минуты
7. Обновите страницу `https://3469335.github.io/ChatList/`

## 📝 Проверка статуса деплоя

1. Перейдите в Settings → Pages
2. Внизу страницы вы увидите статус последнего деплоя
3. Если есть ошибки, они будут отображены там

## 🚨 Частые проблемы

### Проблема: "Page build failed"
- Проверьте синтаксис HTML в `docs/index.html`
- Убедитесь, что нет бинарных файлов в docs/

### Проблема: "404 Not Found" после настройки
- Подождите 1-5 минут (GitHub Pages может обновляться с задержкой)
- Очистите кэш браузера (Ctrl+F5)
- Проверьте URL: должен быть `https://3469335.github.io/ChatList/` (без `/docs/`)

### Проблема: Workflow не запускается
- Убедитесь, что файл `.github/workflows/pages.yml` существует
- Проверьте синтаксис YAML
- Убедитесь, что workflow закоммичен в репозиторий

## 📞 Дополнительная помощь

Если проблема не решается:
1. Проверьте [GitHub Pages документацию](https://docs.github.com/en/pages)
2. Посмотрите логи в Settings → Pages (внизу страницы)
3. Проверьте Actions → Deploy to GitHub Pages для ошибок
